import { IInfoYoutube } from '../../types/infoYoutube';

interface props {
    info: IInfoYoutube
}

export const VideoInfo = ({ info }: props) => {
    return (
        <section className='section-video-info'>
            
        </section>
    )
}